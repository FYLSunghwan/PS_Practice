#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int maxs[i
    while(N--) {
        ++cnt;
        cin >> T;
        if(T>ax) {
            ax = T;
            argmax=cnt;
        }
    }
    cout << ax << endl << argmax;
    return 0;
}