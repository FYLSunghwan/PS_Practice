#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll T;

int main() {
    cin >> T;
    while(T--) {
        ll x, a, b;
        cin >> x;
        cout << 1 << " " << (x-1) << endl;
    }
    return 0;
}